"""VLM 抽出用プロンプト。

ステータス名は src/tools/zzz_disc/schema.py の
SLOT_ALLOWED_MAIN_STATS / 異常掌握 / hoyolab_client._NAME_ALIASES に合わせて
正規形で返させる。ここで表記ゆれを吸収しておくと、保存時バリデーションで
弾かれないで済む。
"""

EXTRACT_PROMPT = """あなたはゲーム「ゼンゼロ（Zenless Zone Zero）」のディスク詳細画面のスクリーンショットから情報を抽出するアシスタントです。

以下の JSON スキーマで結果を返してください。画像に存在しない項目は省略し、null や空文字列は入れないでください。

{
  "slot": <1-6の整数。「部位 N」「位置 N」等の表記から判定>,
  "set_name": "<ディスクセット名、日本語のまま>",
  "rarity": "<S / A / B のいずれか>",
  "level": <0-15の整数。現在のレベル>,
  "main_stat": {"name": "<下記のいずれか>", "value": <数値>},
  "sub_stats": [
    {"name": "<下記のいずれか>", "value": <数値>, "upgrades": <強化+N回数、0-5の整数>}
  ]
}

ステータス名の候補（name に使える値。必ずこの表記で返す。% は末尾に付けた形で）:
- フラット値: HP / 攻撃力 / 防御力 / 貫通値 / 異常マスタリー / 異常掌握
- パーセント: HP% / 攻撃力% / 防御力% / 会心率% / 会心ダメージ% / 貫通率% / 衝撃力% / エネルギー自動回復%
- 属性ダメージ: 物理属性ダメージ% / 炎属性ダメージ% / 氷属性ダメージ% / 電気属性ダメージ% / エーテル属性ダメージ%

重要な正規化ルール:
- 「雷ダメージ」は「電気属性ダメージ%」と表記する。
- 「物理ダメージ」「炎ダメージ」「氷ダメージ」「エーテルダメージ」もそれぞれ「○○属性ダメージ%」に統一。
- 「貫通率」「衝撃力」「エネルギー自動回復」「会心率」「会心ダメージ」は必ず末尾に % を付ける。
- 「異常マスタリー」と「異常掌握」は画像に合わせてそのまま返す（どちらも可）。

その他ルール:
- sub_stats は最大 4 件。画像に見える分だけ。
- upgrades は「+N」表記の N（ドット数やローマ数字でも 0-5 の整数に変換）。例: "攻撃力 +38 (2)" なら upgrades=2。表記が無ければ 0。
- value は数値のみ。"%" は付けない（name 側に % が含まれている）。
- JSON 以外の文字列（前置き、マークダウンコードブロック等）は絶対に出力しない。
"""
